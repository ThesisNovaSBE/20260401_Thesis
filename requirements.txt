# Core
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.4   # HistGradientBoostingClassifier(class_weight=...) needs >=1.4
xgboost>=2.0
optuna>=3.4
joblib>=1.3
pyyaml>=6.0
python-dotenv>=1.0

# Stage 2 — Clinical-Longformer fine-tuning
transformers>=4.36
torch>=2.1
accelerate>=0.27       # required by HuggingFace Trainer

# Stage 3 — local LLM explanations + cross-modal analysis
ollama>=0.3
shap>=0.44   # SHAP values for Stage 1 feature attribution (falls back to gain importances)

# Notebooks / EDA
matplotlib>=3.7
seaborn>=0.13
jupyterlab>=4.0

# API server
fastapi>=0.110
uvicorn[standard]>=0.27

# Testing
pytest>=7.0

# Utilities
tqdm>=4.65
