# Session Log

- **Date:** 2026-07-06
- **Author:** Martin Schmitz (with Claude)
- **Branch:** main

## What I worked on

A full-repository sanity check: code status, narrative/research-gap critique, and a
prioritised backlog with ready-to-paste AI prompts. Deliverable:
`docs/SANITY_CHECK_2026-07-06.md`. No code changed.

## Key findings

- **Code is ahead of the evidence.** Local Stage 1 artifact (`stage1_xgboost.joblib`) is
  still the June-24 synthetic quick-mode model; `data/processed/` is empty; the MODEL_CARD
  headline numbers come from a teammate's machine and are not reproducible here.
- **Stage 2 code ↔ results mismatch.** The rebuilt Stage 2 (2048 tokens, focal loss,
  age-stratified sampling, Platt calibration — session 09) is coded but **not trained**;
  MODEL_CARD still reports the old v1 (15k notes, 512 tokens, AUROC 0.640).
- **Stage 1 3-model comparison never run on real data** (only XGBoost).
- **Stage 3 never run**; frontend uses mock patients.
- **Vitals come from `icu/chartevents` (ICU-only)** → large unquantified missingness for
  non-ICU admissions; lab itemids unvalidated against `d_labitems`.
- **Feature cache is mode-blind** (the trap that served synthetic data to a real run earlier).
- **No statistical rigour** (no CIs, no DeLong test, no Stage 1 calibration curve), **no
  interpretability**, **no tests**, **no EDA artifacts**.

### Narrative gaps
- Cascade vs. joint multimodal model is unjustified (needs rationale + ablation).
- "Why not notes-only?" → needs the disagreement analysis (Stage 1 flags that Stage 2 overturns).
- Notes-cohort denominator can look favourable → always report full-cohort recall too.
- Clinical "so what" unquantified → decision-curve / cost analysis + alert-fatigue framing.
- Stage 3 unevaluated (faithfulness) — good candidate for the expert interviews.

## Files changed

- Created: `docs/SANITY_CHECK_2026-07-06.md` (status quo + code issues C1–C12 + narrative
  gaps N1–N7 + prioritised backlog + one AI prompt per task)
- Created: `docs/THESIS_NARRATIVE.md` (same session, later) — storyline options A–E,
  recommended blend (alert-fatigue lead + second-reader structure + disagreement-analysis
  depth), chapter-by-chapter red thread, storytelling devices (composite patient vignette,
  alerts-per-true-catch metric, reusable funnel diagram), a ~400-word abstract for
  Prof. Shen, and prepared answers to likely committee questions.
- Reworked: `docs/SANITY_CHECK_2026-07-06.md` → **Revision 2** (same session, later).
  Status updated (C3 in progress by Martin; C2 blocked — Stage 2 training aborted on a
  teammate's laptop, GPU plan + reduced-scale fallback now part of the C2 prompt). All
  prompts aligned with the THESIS_NARRATIVE strategy (second-reader framing, dual
  denominators, alerts-per-true-catch, session-log discipline baked into every prompt).
  New tasks added: `M1` alert-burden metrics, `N8` temporal validation, `W1` LaTeX chapter
  skeleton, `W2` composite vignette, `W3` reusable funnel figure, `W4` claims traceability
  audit, `F1` frontend sync with real results.
- Created: this session log

## What works now

- Unchanged from session 09 (no code touched). The sanity-check doc is the working artifact.

## Next steps (from the doc, in priority order)

1. Tier 1 — make evidence real: `C1` reproducible run + manifest, `C3` Stage 1 3-model
   comparison, `C2` retrain rebuilt Stage 2 on GPU, `C9`/`N3` cascade fallback + full-cohort
   metrics.
2. Tier 2 — make it defensible: `C4` CIs + DeLong + calibration, `N1`/`N2` ablation +
   disagreement analysis, `C5` interpretability, `C6` feature audit, `N4` decision-curve.
3. Tier 3 — complete & robust: `C8` tests, `C10` EDA, `N6` Stage 3 + faithfulness, `C7`
   mode-aware cache, `N5`/`N7` writing.

Highest-leverage single item: **N2 disagreement analysis** wrapped in **N4 alert-fatigue
framing** — answers "why does the LLM layer matter?".
