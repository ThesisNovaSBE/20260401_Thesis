# Session Log

- **Date:** 2026-07-26 to 2026-07-30
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** stage-2-rebuilding

## What I worked on

Three-day session focused on getting Stage 2 training running on a real GPU (GWDG KISSKI HPC cluster) instead of local CPU. The rebuilt Stage 2 (session 09) had never been trained on real data — this session closes that gap.

### Phase 1 — Config scaling for GPU (2026-07-26)

Decided to use the GWDG KISSKI cluster (A100 80GB) instead of a local RTX 3080 (10GB). Scaled training config accordingly:

- Training notes: 35k → 100k → **249k** (~99% of available MIMIC-IV-Note training data)
- Age-group targets all scaled 2.5×; 70+ group remains oversampled for fairness
- `batch_size`: 4 → **8** (A100 80GB has no VRAM constraint)
- `gradient_accumulation_steps`: 4 → 2 (effective batch still 16)
- `gradient_checkpointing`: true → **false** (not needed with 80GB)
- `fp16`: true → **false**; `bf16`: false → **true** (A100 native format)
- `dataloader_num_workers`: 4 → **8**
- `save_steps: 500` retained for crash recovery (~12 min worst-case loss)

### Phase 2 — GWDG KISSKI cluster setup (2026-07-26 to 2026-07-29)

Full cluster setup from scratch:

| Detail | Value |
|--------|-------|
| Cluster | GWDG KISSKI (AI/ML-specific) |
| Username | u29346 |
| Login node | glogin-gpu.hpc.gwdg.de |
| SSH alias | `ssh kisski` (via `~/.ssh/config`) |
| Project dir | `/projects/extern/kisski/kisski-nova-rpcl/dir.project` |
| GPU assigned | NVIDIA A100-SXM4-80GB (81,920 MiB) |
| Partition | `kisski` |
| Python env | `thesis-env` (conda, Python 3.11, PyTorch + CUDA 12.1) |

Data transferred:
- MIMIC-IV v3.1 (9.9 GB) → `$PROJECT_DIR/mimic-iv/`
- MIMIC-IV-Note v2.2/note (1.8 GB) → `$PROJECT_DIR/mimic-iv-note/`
- Stage 1 artifact (4.4 MB) → `~/thesis/models/stage1_xgboost.joblib`

### Phase 3 — Debugging (2026-07-29 to 2026-07-30)

Two jobs (15067448, 15098411) failed within 26 seconds, both reporting ExitCode 0 incorrectly. Root causes found and fixed:

**Bug 1 — `os.environ["CUDA_VISIBLE_DEVICES"] = ""`**
Location: `setup_stage2.py` line 46 (pre-fix).
This was a macOS workaround to prevent MPS from conflicting with joblib. On Linux/cluster it disabled the GPU entirely, silently forcing CPU training.
Fix: wrapped in `if sys.platform == "darwin":` so it only fires on Mac.

**Bug 2 — Dict subscript access on Pydantic AppConfig**
Location: `setup_stage2.py`, `check_prerequisites()` and `main()`.
Multiple occurrences of `cfg["stage1"]["model"]`, `cfg["data"].get(...)`, `cfg["run"]["mode"]`, `cfg['stage2']['model_name']`, etc. Pydantic v2 models are not subscriptable — these all raise `TypeError: 'AppConfig' object is not subscriptable`.
Fix: converted all to dot access (`cfg.stage1.model`, `cfg.run.mode`, etc.).

**Bug 3 — No `set -e` in bash job script**
`train_stage2.sh` had no `set -e`. When Python exited with code 1, bash continued to `echo "Finished"` and exited with code 0. Slurm reported ExitCode 0, hiding the crash entirely.
Fix: added `set -e` and `set -o pipefail` to the job script header.

**Bug 4 — Stage 1 artifact missing on cluster**
`models/` was excluded from the initial rsync. The script hit `sys.exit(1)` immediately on artifact check — masked by Bug 3.
Fix: rsync `models/stage1_xgboost.joblib` separately to the cluster.

**Bug 5 — Missing `set -e` masking all errors**
See Bug 3. Combined with Bug 4, this caused two 26-second "successful" failures that each wasted a ~16-hour wait in the job queue.

### Phase 4 — Pre-flight validation added

New `train_stage2.sh` now includes explicit pre-flight checks before launching Python:
- `ls models/stage1_xgboost.joblib` (fails fast if artifact missing)
- Python check that MIMIC-IV-Note path exists
- Python check that CUDA is available and prints GPU name + VRAM

## Key decisions made

- **KISSKI over local GPU**: KISSKI A100 80GB has 8× the VRAM of the RTX 3080, allowing larger batch size and no gradient checkpointing. Training time per epoch is comparable despite 2.5× more data.
- **249k notes**: Use ~99% of available training data (vs. 40% at 100k). Cell caps set high enough that they hit natural limits; 70+ group still oversampled.
- **bf16**: A100 supports bf16 natively — more numerically stable than fp16 and no loss scaling needed.
- **Crash recovery**: Step-level checkpointing every 500 steps (~12 min intervals) retained from previous session. If job hits 48h wall-time limit, resubmit without changes — auto-resumes from latest checkpoint.

## Files changed

- `config.yaml` — scaled age_group_train_targets 2.5×; bf16=true, fp16=false, batch_size=8, gradient_checkpointing=false, workers=8
- `src/config_schema.py` — `save_steps: int = 500` added to Stage2Config
- `src/stage2/train.py` — step-level checkpointing, `_find_resume_checkpoint` helper, crash-recovery wired into `trainer.train()`
- `setup_stage2.py` — fixed CUDA env var (macOS-only), all dict subscript → dot access, `check_prerequisites` fixed
- `train_stage2.sh` — created; Slurm job script with `set -e`, pre-flight checks, A100 80GB request
- `docs/HPC_DEPLOYMENT.md` — created; full reproducible setup guide for KISSKI

## What works now

- KISSKI cluster fully set up; conda env `thesis-env` active with PyTorch + CUDA 12.1
- MIMIC data on cluster at `$PROJECT_DIR/mimic-iv/` and `$PROJECT_DIR/mimic-iv-note/`
- Stage 1 artifact on cluster at `~/thesis/models/stage1_xgboost.joblib`
- All known crash bugs fixed; pre-flight checks will surface any remaining issue within seconds
- Training job submitted (ID 15098411 was last confirmed submitted; new job pending as of 2026-07-30)

## What's broken / open problems

- Stage 2 training not yet confirmed running successfully end-to-end (was pending at end of session)
- MODEL_CARD Stage 2 metrics are from v1 (15k notes, CPU, old architecture) — will be outdated once new training completes
- After training: need to run `python -m src.stage2.calibrate` and `python -m src.stage2.evaluate` then update MODEL_CARD

## Next steps for whoever continues

1. Confirm job is running: `squeue -u u29346` — should show `R`
2. Monitor: `tail -f $PROJECT_DIR/logs/stage2_<jobid>.log`
3. If it crashed again: `cat $PROJECT_DIR/logs/stage2_<jobid>.err` and fix
4. After training completes: `rsync` the model back to Mac (see `docs/HPC_DEPLOYMENT.md` Step 14)
5. Run calibration and evaluation locally: `python -m src.stage2.calibrate && python -m src.stage2.evaluate`
6. Update MODEL_CARD.md Stage 2 section with new metrics
7. Run Stage 3: `python setup_stage3.py --limit 500`
8. Update MODEL_CARD.md Stage 3 section with discordance analysis results
9. Update frontend mock data with calibrated per-group thresholds

## Stage 3 redesign (2026-07-31)

Carried out in this session while Stage 2 training was queued/running.

**Problem with old Stage 3:** narrated Stage 1 XGBoost features via phi4-mini — essentially SHAP explanation + LLM, a well-trodden pattern with no novel finding. Stage 2 was completely invisible to it.

**New design — Cross-Modal Discordance Analysis:**

Stage 3 now processes ALL Stage 1 flagged patients (confirmed AND rejected by Stage 2).  For each patient it feeds phi4-mini:
- Top-k SHAP features from Stage 1 (structured risk drivers)
- Top-n sentences from the discharge note with highest Clinical-Longformer attention weight (Stage 2's internal signal, extracted via `output_attentions=True`)
- Stage 2 decision + confidence

phi4-mini returns JSON: `discordance_mode` + `primary_category` + `explanation`.

Population-level aggregation of these annotations across the full test set produces the thesis research finding: **which clinical domains in discharge notes systematically explain the predictive gap between structured EHR data and free-text clinical notes**.

**Files created/changed:**
- `src/stage3/attention.py` — NEW: Longformer attention span extraction
- `src/stage3/shap_extract.py` — NEW: SHAP feature extraction from Stage 1
- `src/stage3/explain.py` — REWRITE: cross-modal prompt + JSON output
- `src/stage3/categorize.py` — NEW: population-level aggregation
- `src/stage3/run.py` — REWRITE: new pipeline orchestration
- `setup_stage3.py` — REWRITE: fix Pydantic dict-access bugs + new pipeline
- `src/config_schema.py` — 4 new Stage3Config fields
- `config.yaml` — 4 new stage3 settings
- `requirements.txt` — added `shap>=0.44`
