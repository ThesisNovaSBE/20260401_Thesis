# Session Log

- **Date:** 2026-08-28
- **Author:** Claude (continued from session 18)
- **Branch:** training-changes

## What I worked on

A colleague independently wrote up a proposed pipeline restructuring
("Thesis Pipeline — Agreed Structure & Next Steps"). Evaluated it in full
against the current implementation (report delivered separately, not
written to a file): most of it matched what session 18 had already built,
or restated decisions already recorded in ARCHITECTURE.md §5. The
genuinely new material was Stage 3's output schema (a fixed mitigating/
aggravating grounds taxonomy, a second code-computed decision alongside the
LLM's own) and two new validation controls. Built the approved BUILD NOW
items from that evaluation, resolved two open questions along the way, and
updated every doc that described the old schema.

## Key decisions made

- **Conditional Stage 3 triggering: resolved, not adopted as the execution
  mode.** `run_batch_audit` still audits every Stage 1-flagged, note-covered
  admission (unchanged). Discordant-only triggering is answered instead as
  a post-hoc analysis over a completed blanket run
  (`_conditional_triggering_report`) — committing to conditional as the
  design would mean never learning what auditing concordant cases finds,
  and no full batch run has happened even once yet, so there's no cost
  currently being saved by skipping it. The cost/benefit trade-off (LLM
  calls saved vs. metric delta) is now a measured, reportable result
  instead of a design assumption.
- **Operating point K: stays at 0.15 (primary).** A colleague's proposal to
  make K=20% primary had no comparable justification to the existing
  clinical-capacity argument behind 0.15 (session 15). Not changed without
  new operational evidence. 20%/10%/5% remain secondary
  `capacity_report_points`, unchanged.
- **Grounds taxonomy replaces the single `primary_clinical_domain`.** Ten
  fixed grounds (4 mitigating, 6 aggravating), each requiring its own
  verbatim quote, independently verified. Fixed list only — an invalid
  ground or empty quote fails the whole response, same enforcement level
  the old domain/decision/quote fields already had.
- **Two decisions, not one: `decision_model` (the LLM's own free judgment)
  and `decision_rule` (the same three-way decision recomputed
  deterministically in code from the extracted grounds).** This resolves
  the tension ARCHITECTURE.md §5 item 3a had left unresolved (small
  additions to the schema vs. replacing the free decision with pure
  extraction) — `decision_rule` gives a deterministic, human-checkable
  fallback without ever replacing `decision_model` as the value that drives
  the pipeline's final prediction.
- **`insufficient_evidence` added as a third decision value**, computed
  code-side from note length (`_MIN_INFORMATIVE_NOTE_CHARS`) for
  `decision_rule`, and as a real option the LLM itself can pick for
  `decision_model`. `evaluate_pipeline.py:_apply_stage3_decisions` updated
  to treat it as "no usable coverage" (falls back to the prior C9-corrected
  prediction), not silently miscounted as either uphold or override.
- **Prompt field order fixed independently of the rest of the migration.**
  The prior prompt asked for `decision` before the supporting quote/
  justification — a real autoregressive-conditioning bug (the colleague's
  critique was correct, verified against the actual template, not just
  plausible). Grounds/evidence are now requested before decision.
- **Schema-constrained Ollama generation** (`format=<JSON schema>`) replaces
  the generic `format="json"` — the installed `ollama` client (0.6.1)
  supports this; not yet measured whether it reduces the parse-failure rate
  since no real batch run has happened yet.
- **`supporting_quote`/`quote_verified` dropped**, superseded by per-ground
  quotes + an aggregate `all_quotes_verified`. `planned_return` stays a
  separate, always-answered field — deliberately not merged into the
  grounds list, even though "planned_return" also exists as a mitigating
  ground the model may cite when it drives the decision.
- **Two validation controls added as diagnostic functions, not wired into
  the default batch path:** blind-note control (rerun with the note
  withheld — tests whether the auditor is actually reading it) and
  no-Stage-2 control (rerun with Stage 2's score/discordance withheld —
  tests whether Stage 2 earns its place in the prompt). Both implemented by
  adding `suppress_note`/`suppress_stage2` parameters to `explain_patient`
  and a `hide_stage2` parameter to `build_prompt`, not a separate duplicated
  code path.
- **Self-agreement check added** (temperature=0 has never been empirically
  verified to actually produce identical output across two calls) — a
  standalone diagnostic to run once before a full Phase C5 batch run.
- **A real gap found, not fixed this session:** no included-vs-excluded
  (notes-covered vs. not) selection-bias comparison table exists anywhere
  in the repo. Recorded in ARCHITECTURE.md §4 as deferred, not built.

## Files changed

- `src/stage3/explain.py` — new `MITIGATING_GROUNDS`/`AGGRAVATING_GROUNDS`
  (replacing `CLINICAL_DOMAINS`), `DECISIONS` gains `insufficient_evidence`,
  new `_LLMOutput`/`_GroundHit` Pydantic models (schema-constrained
  generation + parsing), `compute_decision_rule`, `is_note_truncated`,
  reordered `_USER_TEMPLATE`, `build_prompt` gained `hide_stage2`,
  `_parse_response` rewritten for grounds validation, `call_llm` computes
  per-ground `quote_verified` + `all_quotes_verified` + `decision_rule`.
- `src/stage3/models.py` — `ExplanationResult` restructured: new `GroundHit`
  model, `mitigating_grounds`/`aggravating_grounds`/`all_quotes_verified`/
  `decision_model`/`decision_rule`/`note_truncated`/`model_name`; dropped
  `primary_clinical_domain`/`supporting_quote`/`quote_verified`/`decision`.
- `src/stage3/pipeline.py` — `explain_patient` gained `suppress_note`/
  `suppress_stage2`; resolves and persists `model_name`; computes
  `note_truncated`.
- `src/stage3/batch.py` — `_OUTPUT_FIELDS` updated (grounds JSON-serialized
  for CSV); new `run_blind_note_control`, `run_no_stage2_control` (shared
  `_run_control` helper), `check_self_agreement`.
- `src/model/evaluate_pipeline.py` — `_apply_stage3_decisions` reads
  `decision_model`, treats `insufficient_evidence` as no-coverage, reports
  `n_insufficient_evidence`; new `_conditional_triggering_report`, wired
  into `evaluate_pipeline()`'s output as `pipeline.conditional_triggering`.
- `tests/test_stage3_explain.py`, `test_stage3_batch.py`,
  `test_evaluate_pipeline.py` — rewritten/extended for the new schema and
  the four new functions.
- `frontend/src/types.ts`, `frontend/src/components/PatientModal.tsx` —
  matched to the new `ExplanationResult` shape (new `GroundHit` type,
  grounds lists with per-ground verified quotes, two decision badges,
  disagreement warning when `decision_model` != `decision_rule`).
- `docs/ARCHITECTURE.md` — §2 (Layer 3) rewritten for the new schema; §5
  updated (conditional triggering + K resolved, moved out of "still
  needed"); §4 gained the selection-bias table gap; new §3 session-19 entry.
- `docs/STAGE3_VALIDATION.md` — re-targeted (its own banner had said this
  was needed but not yet done): kappa on `decision_model`, set-overlap
  agreement on grounds instead of percent agreement on a single category.
- `MODEL_CARD.md` — Stage 3 output-schema table and grounds taxonomy
  updated; corrected two other stale claims found in the same section
  ("not yet run in batch" / "batch runner does not exist yet" — both have
  existed since session 17).
- `CITATION.cff` — abstract's Stage 3 description updated for precision
  (grounds, two decisions) without a full rewrite — the framing from
  session 18's fix still held at that level of abstraction.

## What works now

- Full backend test suite: 143 passed, 1 skipped (unrelated `shap`
  import-skip). Pylint: 10.00/10.
- Frontend: `tsc --noEmit` clean.
- Every Stage 3 field name is now consistent end-to-end: backend model →
  batch CSV output → API response → frontend types/rendering. (This was
  exactly the class of bug found and fixed in the prior repo-audit pass —
  checked deliberately this time so it wasn't reintroduced.)
- `_conditional_triggering_report` and the two validation-control functions
  are real, tested, callable functions — not yet run against real data,
  since no GPU retrain or full batch run has happened.

## What's broken / open problems

- Everything already deferred in ARCHITECTURE.md §4 is still deferred
  (Stage 1/Stage 2 retrain, `predict_stage2_all`/`compare_layers.py`/
  `batch.py` at full scale, bootstrap CIs in `evaluate_pipeline.py`,
  `readmission_30d_unplanned` unused, feature audit, macOS
  `stage2.predict` segfault, frontend `confirmedOnly` default).
- New: the included-vs-excluded selection-bias table does not exist yet
  (§4).
- `stage3.discordance_displacement_pp` (20pp) is still provisional/
  unvalidated — unaffected by this session, still needs `batch.py --sweep`
  against a real run.
- Whether schema-constrained generation (`format=<schema>`) actually
  reduces the parse-failure rate is unmeasured — no real batch run has
  used it yet.
- The three remaining open architecture questions from session 18 (split
  parity between Stage 1/Stage 2, what exactly Stage 3 reasons over beyond
  the current bundle, per-subgroup context) are still open, untouched.

## Next steps for whoever continues

- When GPU time is available: retrain Stage 1 and Stage 2, then run the
  full pipeline (`predict_stage2_all` → `compare_layers.py` → `batch.py` →
  `evaluate_pipeline.py`) to get the first real numbers for RQ1, RQ2, the
  control arm, and the new conditional-triggering post-hoc analysis.
- Before the full Phase C5 batch run: run `check_self_agreement` on a small
  sample first (this was the whole point of building it this session).
- Once a real batch run exists: run `run_blind_note_control` and
  `run_no_stage2_control` on a sample and compare decision distributions
  against the real run — this is the actual validation, not just having
  the functions exist.
- Build the included-vs-excluded selection-bias table (§4) — flagged this
  session, not started.
