# Session Log

- **Date:** 2026-07-05
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** front-end

## What I worked on

- Built the React + TypeScript + Vite + Tailwind frontend from scratch
- Updated README.md with full pipeline results and frontend instructions
- Fixed git index corruption (stale lock file + truncated index)
- Updated `.gitignore` to properly exclude large model binaries

## Frontend built (`frontend/`)

**Stack:** React 18 + TypeScript + Vite + Tailwind CSS (same as colleague's ATML_Sepsis_Alert project)

**Two views:**

1. **Pipeline tab** (main view for professor meeting):
   - Visual Stage 1 → Stage 2 → Stage 3 diagram with real metrics per stage
   - "How it works" section with plain-language explanation of each stage
   - 4 key metric cards (AUROC, Recall, Precision gain, F2)
   - Designed to explain the full pipeline logic to a non-technical audience

2. **Patients tab** (clinical dashboard):
   - Sortable table of confirmed high-risk patients (by Stage 1 score, Stage 2 score, age)
   - Risk badges (color-coded by severity), score bars, readmission ground truth
   - Click any row → modal with full patient profile + Stage 3 narrative explanation
   - 8 synthetic representative patients with realistic clinical explanations

**Files created:**
- `frontend/src/App.tsx` — Main layout + tab navigation + modal state
- `frontend/src/types.ts` — Patient type definition
- `frontend/src/index.css` — Tailwind directives (replaced Vite boilerplate)
- `frontend/src/components/PipelineDiagram.tsx` — Stage 1/2/3 boxes with metrics
- `frontend/src/components/PatientTable.tsx` — Sortable patient table
- `frontend/src/components/PatientModal.tsx` — Patient detail with score gauges + explanation
- `frontend/src/data/mockPatients.ts` — 8 synthetic patients + real pipeline metrics

## Key design decisions

- **Synthetic patients only in frontend** — stage2_results.csv is gitignored (MIMIC data policy); mock data reflects real pipeline statistics (precision=0.309, readmission rate=25.6%)
- **Pipeline tab first** — professor meeting is the immediate use case
- **No backend** — pure static frontend; real data could be loaded via CSV import or a thin Flask API later

## `.gitignore` fixes

Added proper exclusions for large model files that were missing:
- `*.safetensors` — Clinical-Longformer weights (~500MB each)
- `models/stage2_checkpoints/` — 4 training checkpoints (~2GB total)
- `models/stage2_longformer_best/model.safetensors` + `training_args.bin`
- `models/**/*.pt` / `models/**/*.bin` — recursive patterns (was only matching one level deep)

## README updated

- Added real Stage 1 and Stage 1+2 results tables
- Added frontend quick start section
- Added utility scripts table
- Updated project structure tree to include Stage 2/3 and frontend

## Run the frontend

```bash
cd frontend
npm install
npm run dev   # http://localhost:5173
```

## Next steps

1. Run `python setup_stage3.py --limit 50` to get real Stage 3 explanations → replace mock explanations in `frontend/src/data/mockPatients.ts`
2. Show professor the Pipeline tab — explains Stage 1→2→3 with real numbers
3. Consider adding a chart (Recharts already installed) showing the threshold sweep table visually
4. Merge front-end branch into main after professor review
