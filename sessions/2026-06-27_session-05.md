# Session Log

- **Date:** 2026-06-27
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** stage_2_3_implementation

## What I worked on

- Deep diagnostic review of all pipeline stages (Stage 1–3) before running Stage 2
- Found and fixed 7 bugs spanning data integrity, segfaults, wrong indices, and thesis quality
- Configured Stage 2 for feasible CPU training: 15k training notes + 3k eval cap
- Verified all prerequisites for `python setup_stage2.py --mode full`

## Key decisions made

**Stage 2 training strategy:**
- Fine-tune Clinical-Longformer on a stratified 15k-note subsample (out of 251k available)
- Cap per-epoch eval to 3k notes so periodic checkpointing does not dominate wall time
- Reasoning: full 251k notes × 5 epochs × periodic eval = 46+ days on CPU; 15k ≈ 13 hours overnight
- 15k notes is ~6% of the full training set — representative, stratified, methodologically valid
- Report methodology clearly in thesis: sample size and motivation are part of the contribution

**max_seq_length stays at 512:**
- Clinical-Longformer supports 4096 tokens; MIMIC-III discharge notes average ~1900 tokens
- Bumping to 1024 would make per-step training ~11x slower (memory pressure on Apple Silicon)
- 512 tokens captures the beginning of discharge summaries where diagnosis/plan appear
- Noted as a limitation: full context window only feasible on GPU

**Threshold (Stage 2 confirmation):**
- Kept at default 0.5 — needs tuning after training completes
- Run `python -m src.stage2.predict --threshold X` to experiment with thresholds

## Files changed

- `src/stage2/train.py` — artifact pre-loading; use artifact["mode"] not cfg mode; max_train_notes subsampling; max_eval_notes eval cap; removed unused test_dataset
- `src/stage2/predict.py` — artifact parameter; use artifact["mode"]; threshold None-check fix
- `setup_stage2.py` — pre-load artifact BEFORE torch import; pass artifact to both train/predict; summary bug fix; remove unused has_real_data import
- `config.yaml` — max_seq_length stays 512; added max_train_notes: 15000; max_eval_notes: 3000; epochs: 5
- `diagnose_stage2.py` — guard: reuse full-mode Stage 1 artifact instead of overwriting it with quick-mode re-train
- `src/stage3/run.py` — always load full-mode matrix for Stage 3 prompt context
- `MODEL_CARD.md` — Stage 1 full metrics added (AUROC=0.706, AUPRC=0.406, TP=17864, FP=51911)

## What works now

- Stage 1: fully trained on 521,191 real MIMIC-IV admissions (full mode)
  - AUROC=0.7058, AUPRC=0.4057, recall=0.848 @ threshold=0.354
  - Fairness: Female=0.705, Male=0.704 (equal); 70+ AUROC=0.660 (limitation noted)
- Stage 3: phi4-mini via Ollama generates coherent patient risk explanations
- All segfaults fixed (joblib loaded before torch in setup_stage2.py)

## What's broken / open problems

- Stage 2 results are TBD — training runs overnight
- Stage 2 threshold (0.5) needs tuning after training to optimise precision/recall trade-off
- After training, run `python -m src.stage2.predict` to get Stage 1+2 combined precision

## Next steps for whoever continues

1. Let `python setup_stage2.py --mode full` run overnight (~13 hours)
2. Check `models/stage2_metrics.json` for AUPRC/AUROC of the fine-tuned model
3. Check `models/stage2_results.csv` for Stage 1+2 combined precision/recall
4. Update MODEL_CARD.md Stage 1+2 table with real numbers
5. Tune Stage 2 threshold: `python -m src.stage2.predict --threshold 0.4` etc.
6. After threshold is set: run `python setup_stage3.py` for explanations
7. Do NOT run `diagnose_stage2.py` until all stages complete — it is safe now (guard added) but still re-trains Stage 1 in quick mode if no full-mode artifact exists


