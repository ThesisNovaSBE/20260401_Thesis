# Session Log

- **Date:** 2026-06-25
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** stage_2_3_implementation

## What I worked on

Model selection research for Stage 2 (clinical notes encoder) and Stage 3 (explanation
generator), followed by full implementation of both stages.

## Key decisions made

### Stage 2 model: Clinical-Longformer (`yikuan8/Clinical-Longformer`)

Evaluated four candidate models for Stage 2:

| Model | Max tokens | Verdict |
|---|---|---|
| `emilyalsentzer/Bio_ClinicalBERT` (previous default) | 128 | Rejected — truncates most discharge notes |
| `medicalai/ClinicalBERT` | 256 | Rejected — DistilBERT-based, no published benchmarks |
| `yikuan8/Clinical-BigBird` | 4096 | Good, but loses to Longformer on every task |
| `yikuan8/Clinical-Longformer` | 4096 | **Selected** |

Decision based on the paper **Li et al. (2022), arXiv:2201.11838**
("Clinical-Longformer and Clinical-BigBird: Transformers for Long Clinical Sequences",
published as a journal article in JAMIA 2023):

- MIMIC-III discharge summaries average **1,435 words** — far beyond the 512-token BERT limit.
  Bio_ClinicalBERT silently discards the tail of every note (discharge plan, prognosis,
  follow-up instructions — exactly the signals that predict readmission).
- Clinical-Longformer wins on **all 10 benchmarks** in the paper (NER, QA, classification).
- On **MIMIC-AKI** — binary classification from MIMIC clinical notes, avg 1,463 tokens
  (closest available proxy to Stage 2's task):
  ClinicalBERT AUC 0.738 vs Clinical-Longformer AUC **0.762**, F1 0.444 vs **0.484**.
- The authors explicitly recommend Clinical-Longformer over Clinical-BigBird:
  *"Clinical-BigBird also requires more fine-tuning time; we recommend Clinical-Longformer
  for limited resources."*
- The paper explicitly cites readmission prediction as its primary motivation.
- Pre-trained on MIMIC-III clinical notes — same domain as this project.
- `max_seq_length` set to 1024 in config (model max is 4096) for practical MacBook fine-tuning.

### Stage 3 model: phi4-mini via Ollama

Evaluated Meditron-7B (`epfl-llm/meditron-7b`, EPFL) as a medical-domain candidate:

- Meditron-7B is a **base/foundation model — not instruction-tuned**. It continues text
  rather than following prompts, making it unsuitable for Stage 3's prompted generation task.
- Despite being medically pre-trained, instruction-tuned Zephyr-7B (63.3%) and Mistral-7B
  (60.0%) both beat it on MMLU-Medical (54.2%).
- Truthfulness score only 28.3% on medical categories.
- Authors themselves caution against deployment without extensive validation.
- Stage 3 is an **instruction-following + clear writing** task, not medical QA. A well-tuned
  small general model is a better fit than a large unaligned medical base model.
- **phi4-mini** selected: ~3.8B, instruction-tuned, strong reasoning, fast on Apple Silicon,
  available via Ollama (`ollama pull phi4-mini`).

## Files changed

- **Added:** `src/data/__init__.py`, `src/data/cohort.py`, `src/data/comorbidity.py`,
  `src/data/features.py`, `src/data/synthetic.py`
  *(these existed in the working folder from Session 2 but were never committed — added now)*
- **Created:** `src/stage2/__init__.py`
- **Created:** `src/stage2/dataset.py` — loads MIMIC-IV-Note discharge notes, joins with labels,
  `ClinicalNotesDataset` PyTorch Dataset class
- **Created:** `src/stage2/train.py` — fine-tunes Clinical-Longformer with class-weighted loss,
  reuses Stage 1 patient-level split, HuggingFace Trainer + early stopping, MPS/CUDA/CPU support
- **Created:** `src/stage2/predict.py` — loads Stage 1 flagged patients, runs Clinical-Longformer
  inference, outputs `models/stage2_results.csv`
- **Created:** `src/stage3/__init__.py`
- **Created:** `src/stage3/explain.py` — Ollama-based explanation generator, structured prompt
  template covering all key risk features, batch generation with per-patient error handling
- **Created:** `src/stage3/run.py` — entry point; merges Stage 2 results with feature matrix,
  calls explain.py, saves `models/stage3_explanations.csv`
- **Updated:** `config.yaml` — stage2 model → Clinical-Longformer, added `max_seq_length: 1024`,
  `gradient_accumulation_steps: 8`, `threshold: 0.5`; stage3 model → phi4-mini; rationale comments
- **Updated:** `requirements.txt` — added `accelerate>=0.27`, `ollama>=0.3`
- **Updated:** `MODEL_CARD.md` — updated Stage 2/3 model names

## What works now

- `src/data/` is now in the repo (was missing from prior commits despite running correctly)
- Full Stage 2 pipeline ready to run when MIMIC-IV-Note is available:
  `python -m src.stage2.train` → `python -m src.stage2.predict`
- Full Stage 3 pipeline ready once Ollama is set up:
  `ollama pull phi4-mini` → set `stage3.enabled: true` → `python -m src.stage3.run`

## What's broken / open problems

- Stage 2 requires real MIMIC-IV-Note — cannot run on synthetic data (no synthetic notes).
  Path assumed: `<MIMIC_IV_NOTE_DIR>/note/discharge.csv.gz` — verify against actual data.
- Stage 2 `max_seq_length: 1024` covers most notes but a small fraction exceed 2048 tokens.
  Consider bumping to 2048 if memory allows on the team's machines.
- Stage 3 disabled by default (`stage3.enabled: false`). Enable manually when ready.
- No tests for Stage 2 or Stage 3 yet.
- `src/model/predict.py` (Stage 1 standalone inference) still a stub.

## Next steps for whoever continues

1. Verify MIMIC-IV-Note path structure against actual data.
2. Run `python -m src.stage2.train --mode quick` as first smoke-test once notes are available.
3. Tune `stage2.threshold` after training based on the precision/recall trade-off on the
   validation set (lower = prunes more aggressively; higher = more conservative).
4. Enable Stage 3, pull phi4-mini, test explanations on a handful of confirmed cases.
5. Implement `src/model/predict.py` for Stage 1 standalone inference.
6. Add tests for Stage 2 dataset loading and Stage 3 prompt construction.
7. Run all 3 Stage 1 models side-by-side and record results in `MODEL_CARD.md`.
