# Session Log

- **Date:** 2026-07-05
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** stage-2-rebuilding

## What triggered this session

The age fairness analysis (`analyse_age_fairness.py`) revealed two critical problems in the old Stage 2:

1. **Age bias persisted**: Stage 1 had a 6.8 pp precision gap across age groups; Stage 2 barely narrowed it to 6.2 pp
2. **Stage 2 introduced a recall gap**: 70+ recall=0.620 vs 0.751 for younger groups — 13 pp disparity Stage 2 itself created
3. **Root causes**: 512-token limit missed frailty/social context in elderly notes; 15k notes with label-only stratification left only ~820 elderly readmission notes in training; no validation/calibration split; no fairness-aware loss

Decision: full Stage 2 rebuild from scratch with proper methodology.

## Architecture changes

### Data splits (new — was missing entirely)
- Patient-level split (subject_id) to prevent leakage
- 60% fine-tune / 20% validation / 20% calibration — all from Stage 1 training patients
- Stage 1 test set (20% of all patients) locked and used only for final evaluation
- Splits saved as CSVs for reproducibility

### Training data (improved)
- Age×label stratified sampling: 8 cells (4 age bands × 2 labels)
- 70+ oversampled: target 4,500 readmit + 8,000 non-readmit notes
- Total ~45k training notes (up from 15k), all unique (no duplication)

### Sequence length: 512 → 2048 tokens
- Average MIMIC discharge note: ~1,900 tokens
- 512 captured ~27% of the average note (missed frailty, functional status, social sections)
- 2048 captures ~100% of the average note — requires GPU

### Loss function: weighted CE → Focal Loss + age group weights
- Focal loss (γ=2.0): downweights easy examples, upweights hard ones
- Age group loss weights: 70+=1.3×, 56–70=1.1×, younger=1.0×
- Both mechanisms together address the elderly underperformance

### Calibration (new — was missing)
- Platt scaling per age group on calibration set
- Threshold selection: recall_floor≥0.65 per group, then F2-optimal within that constraint
- Calibrated thresholds saved to models/stage2_calibration.json
- Test set used exactly once for final reporting

## Files created / changed

### New files
- `src/stage2/splits.py` — patient-level split builder with age×label stratification
- `src/stage2/calibrate.py` — Platt scaling + per-group threshold selection
- `src/stage2/evaluate.py` — per-age-group metrics, fairness gap, ECE
- `train_stage2_gpu.py` — GPU training script (Colab / HPC compatible)

### Modified files
- `config.yaml` — full stage2 section rewrite (2048 tokens, focal loss, calibration params)
- `src/stage2/train.py` — focal loss trainer, age-stratified sampling, gradient checkpointing
- `src/stage2/dataset.py` — added group_weight support to ClinicalNotesDataset
- `src/stage2/predict.py` — loads calibration JSON, applies per-group thresholds
- `setup_stage2.py` — adds splits + calibration steps to pipeline

### Deleted (obsolete)
- `sweep_stage2_threshold.py` — superseded by src/stage2/evaluate.py
- `update_stage2_threshold.py` — superseded by src/stage2/calibrate.py

## GPU requirements

| Hardware | max_seq_length | batch_size | gradient_checkpointing | Expected time |
|---|---|---|---|---|
| A100 80GB (Colab Pro+) | 2048 | 8 | false | ~4h for 45k notes |
| V100 32GB | 2048 | 2 | true | ~12h |
| RTX 4090 24GB | 2048 | 2 | true | ~14h |
| CPU (Apple Silicon) | not recommended | — | — | weeks |

Config switches for GPU: `bf16: true` (A100), `fp16: true` (V100/consumer), `gradient_checkpointing: true` (if OOM)

## Target metrics (realistic)

| Metric | Old Stage 2 | Target |
|---|---|---|
| Overall AUROC | 0.640 | 0.68–0.72 |
| 70+ recall (notes cohort) | 0.620 | ≥ 0.680 |
| Precision gap (age bands) | 6.2 pp | < 3 pp |
| Recall gap (age bands) | 13.1 pp | < 6 pp |
| Score calibration (ECE) | unknown | < 0.05 |

## Next steps (for whoever continues)

1. Confirm GPU environment (Colab Pro+ A100 recommended)
2. Set hardware flags in config.yaml: `bf16`, `gradient_checkpointing`, `dataloader_num_workers`
3. Run: `python setup_stage2.py --mode full`
   - Step 1 builds splits (fast, CPU)
   - Steps 2–5 require GPU
4. After training: check `models/stage2_calibration.json` for per-group thresholds
5. Final results in `models/stage2_evaluation.json`
6. Update MODEL_CARD.md with new numbers
