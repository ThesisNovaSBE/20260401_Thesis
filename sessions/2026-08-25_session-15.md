# Session Log

- **Date:** 2026-08-25
- **Author:** Lennart Stenzel (with Claude)
- **Branch:** training-changes

## What I worked on

A large architecture-reconciliation session. Reviewed the 46-page PRISMA
literature review (`/Users/lenny/Desktop/20260401_Thesis.pdf`), a colleague's
remediation review (`Thesis_Analysis_Remediation_Plan.pdf`), and three of my
own claude.ai planning artifacts (a methodological evaluation, a Project
Brief, and a Working Paper) against the actual repo code and against each
other. Found that the Stage 3 design had gone through three undocumented
iterations since session 11 and the code was roughly two design-iterations
behind the prose in all three of my own artifacts. Reconciled everything into
one current design (`docs/ARCHITECTURE.md`) and implemented every fix that
did not require GPU retraining.

### Root-cause finding: Stage 3 had already been diagnosed and fixed three times, only in prose

Session 11 (2026-07-31) built "Stage 3 v1" — phi4-mini narrating Stage 1's
own SHAP values — and explicitly rejected it in the same session as "a
well-trodden pattern with no novel finding," replacing it with a
"Cross-Modal Discordance Analysis" design that fed phi4-mini a raw
`stage2_score - stage1_score` delta and let it freely classify a 9-category
taxonomy. Three later planning artifacts (methodological evaluation
2026-08-16, Project Brief 2026-08-17, Working Paper undated) each
independently re-diagnosed this as backwards (discordance should be computed,
not classified by the LLM) and converged on the same fix: phi4-mini as an
independent auditor making its own uphold/override call. None of those three
rounds of fixes had reached the actual code — `src/stage3/explain.py` still
had zero `decision`/`uphold`/`override` fields as of this morning.

### The literature review's own contribution (§2.6) was correctly identified as weak

Confirmed with the user: the lit review's locked §2.6 contribution (XGBoost →
phi4-mini directly, reading SHAP + note) is essentially identical to the
"Stage 3 v1" design the team already tried and rejected in session 11 — an
LLM narrating one model's own explanation is the existing "explainer" role in
the lit review's own concept matrix (Table 3), not the "auditor" role its gap
statement claims to fill. The distinguishing property of a genuine auditor is
reviewing an *independent second model's* output, which requires Stage 2 to
exist and to be informationally independent from Stage 1.

### Fixed the raw-subtraction discordance measure (P1 from the remediation review)

The most recent design docs (Project Brief, Working Paper) proposed
"same isotonic calibration on both stages fixes the raw score-gap problem."
This is an assertion, not a guarantee — two different model families
(XGBoost vs. FusionLongformer) are not guaranteed to have equal residual
miscalibration even under the same calibration method, and Stage 1 currently
has no calibration applied at all (Brier 0.2135, worse than the base-rate
constant). Replaced raw subtraction with percentile-rank displacement within
the flagged+noted cohort — invariant to unequal calibration by construction
(added a test proving this: `test_discordance_invariant_to_monotonic_rescaling`).

### Feature-independence claim resolved without retraining

Stage 1 uses ~40 features (incl. labs/vitals); Stage 2's `STRUCT_FEATURE_COLS`
is a different set of 8. The Working Paper's claim that "both models see the
same structured information" is false against the actual code. Considered
cutting Stage 1 to 8 features to make the claim true — rejected (matches the
remediation review's P5: unjustified retraining cost for no clear benefit).
Resolution: keep both feature sets as they are, drop the "clean attribution
to the note alone" claim, and rely on percentile-rank discordance, which
doesn't need identical feature spaces to be meaningful — it only needs each
score to be a valid within-cohort risk ordering.

### Stage 1 operating point: capacity-constrained, not recall-floor

User flagged (independently, before I'd finished listing it) that Stage 1
flagging 66.94% of admissions at `recall_floor=0.85` is not a deployable
triage — matches remediation review P7a. Added
`select_threshold_for_capacity` / `metrics_at_capacity_points` to
`src/model/metrics.py` and switched Stage 1's primary threshold-selection
policy to flag the top `capacity_k` (default 15%) of admissions by risk
score. Recall-floor is retained and reported as a secondary table for
comparability with prior literature (`stage1.threshold_strategy` config
switch).

### Stage 2 sequence length: 2048 → 4096

Session 9 set `max_seq_length=2048` based on an estimate that the average
MIMIC discharge note was ~1,900 tokens. Session 14 (2026-08-10) measured this
properly while investigating radiology-note concatenation and found 74% of
discharge notes exceed 2048 tokens, median ~2,649 — the number that justified
2048 was already known to be wrong and never got carried back to
`config.yaml`. Clinical-Longformer natively supports 4096 (its whole design
point per Li et al. 2022). Bumped it; this is a config-only change, applies
on the next Stage 2 training run.

## Key decisions made

- Stage 3 (phi4-mini) makes its own independent `uphold`/`override` decision;
  it does not narrate or classify Stage 2's decision.
- Discordance mode is computed quantitatively (percentile-rank displacement,
  `src/stage3/explain.py:compute_discordance`), never by the LLM.
- No claim that Stage 1/Stage 2 share a feature space or that their score gap
  is cleanly attributable to the note alone — they use different information
  by design, and that's the whole point of comparing them.
- Stage 1's primary operating point is capacity-constrained (top-K% by risk),
  not a recall floor. Recall-floor kept as a secondary/comparability table.
- Stage 2's context window is 4096 tokens (was 2048).
- `stage3.temperature` pinned at 0 for all evaluation runs.
- Deliberately did NOT touch `src/stage2/predict.py`'s hard-gating
  (`stage2_confirmed` as a threshold decision) or build a batch Stage 3
  runner this session — both require a coordinated decision about the
  API/frontend contract and are explicitly deferred to the next "exact stage
  behaviour" discussion (see `docs/ARCHITECTURE.md` §4).

## Files changed

- `config.yaml`, `src/config_schema.py` — `stage2.max_seq_length` 2048→4096;
  `stage3.temperature` 0.3→0.0; new `stage1.threshold_strategy`,
  `stage1.capacity_k`, `stage1.capacity_report_points`; new
  `stage3.discordance_displacement_pp`
- `src/model/metrics.py` — added `select_threshold_for_capacity`,
  `metrics_at_capacity_points`; `full_report` now accepts `capacity_points`
- `src/model/train.py` — threshold selection now computes both policies,
  uses the configured strategy; added `_select_thresholds` helper (pylint
  too-many-locals fix)
- `src/model/evaluate.py` — reports capacity-tradeoff alongside recall-tradeoff
- `src/model/evaluate_pipeline.py` — C9 fallback: flagged-no-note patients
  now use Stage 1's flag instead of being silently scored negative; reports
  `pipeline.full_cohort` (primary) and `pipeline.notes_cohort` (secondary)
  separately
- `src/stage3/explain.py` — full rewrite: `compute_discordance` (percentile
  rank), new prompt (independent auditor framing), new output schema
  (`decision`, `primary_clinical_domain`, `clinical_justification`), note
  truncation raised from 1,500 chars to a 20,000-char safety cap, 6-item
  domain taxonomy (was 9)
- `src/stage3/models.py` — `ExplanationResult` updated to new schema
- `src/stage3/pipeline.py` — computes cohort percentile ranks from
  `results_df`, wires into the new `build_prompt`/`compute_discordance`;
  removed now-dead `_get_stage2_threshold`
- `tests/test_stage3_explain.py` — full rewrite for the new API, incl. a
  test that percentile-rank displacement is invariant to monotonic rescaling
- `tests/test_metrics.py`, `tests/test_config.py`, `tests/conftest.py` —
  updated/extended for the new capacity functions and config defaults
- `data/processed/features.csv` — rebuilt with session-14 fixes (fixed
  `admission_type_emergency`, 3 new features); 521,191 rows, 55 model
  features, 20.2% base rate
- `docs/ARCHITECTURE.md` — NEW; single current source of truth for the
  pipeline design, supersedes the three planning docs below
- `docs/IMPLEMENTATION_PLAN.md`, `docs/THESIS_NARRATIVE.md`,
  `docs/SANITY_CHECK_2026-07-06.md`, `docs/STAGE3_VALIDATION.md` — added
  "superseded" banners pointing to `docs/ARCHITECTURE.md`; kept for history
- `README.md`, `PROJECT_TLDR.md`, `MODEL_CARD.md` — updated to current
  module names and Stage 3 design; fixed references to `setup_stage3.py`
  (never existed) and old `src/stage3/categorize.py`/`run.py` (renamed to
  `pipeline.py` on 2026-08-07); results tables marked "pre-retrain"

## What works now

- `python -m pytest tests/` — 65 passed, 1 skipped
- `pylint src/ tests/ api.py` — 10.00/10
- `data/processed/features.csv` is current (session-14 features included)
- `src/stage3/explain.py` / `pipeline.py` / `models.py` implement the
  confirmed independent-auditor design end to end for the on-demand
  (single-patient) path
- `docs/ARCHITECTURE.md` is accurate against the current code as of this
  session

## What's broken / open problems

- Stage 1 has not been retrained against the rebuilt feature matrix or the
  new capacity-constrained policy — `models/stage1_xgboost.joblib` is still
  the old artifact. All Stage 1/pipeline numbers in README/MODEL_CARD are
  pre-retrain.
- Stage 2 has not been retrained at `max_seq_length=4096`.
- `src/stage2/predict.py` and `api.py` (`list_patients(confirmed_only=True)`)
  still treat `stage2_confirmed` as a gate — conceptually inconsistent with
  Stage 2 no longer being a gatekeeper per `docs/ARCHITECTURE.md` §2. Not
  changed this session (would need a coordinated frontend update).
- No batch Stage 3 runner exists. `evaluate_pipeline.py`'s pipeline metric
  still uses `stage2_confirmed` (+ the new C9 fallback) as the final
  prediction, not Stage 3's decision — needed before RQ2/RQ3 can be
  evaluated at scale.
- `stage3.discordance_displacement_pp` (20 percentile points) is provisional
  — needs a percentile-sweep sensitivity check before being reported as a
  methodological choice.
- N1 ablation runner not written. Bootstrap CIs not added anywhere.
- Two smaller, still-open items carried forward unchanged: Stage 1's
  threshold is selected on the same OOF data used for early stopping (mild
  contamination risk); the readmission label counts all-cause returns, not
  just unplanned ones (`readmission_30d_unplanned` variant not built).

## Next steps for whoever continues

1. Decide and implement the Stage 2 gating / batch Stage 3 runner question
   (`docs/ARCHITECTURE.md` §4) — this is the "exact stage behaviour"
   discussion explicitly deferred from this session.
2. Retrain Stage 1: `sbatch scripts/slurm_stage1_tune.sh` (or local `python
   -m src.model.tune && python -m src.model.train`) against the rebuilt
   feature matrix and the new capacity-constrained policy.
3. Retrain Stage 2 at `max_seq_length=4096` on KISSKI/Grete.
4. Once both are retrained, run `python -m src.model.evaluate_pipeline` for
   the first honest full-cohort/notes-cohort C9-corrected numbers.
5. Validate `stage3.discordance_displacement_pp` empirically (percentile
   sweep) before using it in any reported result.
6. Write the N1 ablation runner (5 arms, `docs/ARCHITECTURE.md` references
   the critical "L1 at matched capacity" arm) and add bootstrap CIs.
