# Session Log

- **Date:** 2026-08-28
- **Author:** Claude (session continued from 2026-08-27 / session 17)
- **Branch:** training-changes

## What I worked on

Implemented the "BUILD NOW" items from a five-point colleague review plus the
user's own four open architecture questions, per the user's explicit
instruction to build only what could be built now and leave the open
questions for them to evaluate. Concretely:

1. Control arm — Stage 1 alone, threshold matched to the full system's alert
   volume, as the load-bearing comparison for whether the cascade+audit adds
   value over just raising Stage 1's own threshold.
2. Evidence quoting on every Stage 3 decision, with an automatic verbatim
   verification check against the source note.
3. A `planned_return` question Stage 3 must answer as its own structured
   field, independent of the label-level `readmission_30d_unplanned` proxy.
4. Rewrote `docs/THESIS_NARRATIVE.md` (one-sentence summary, abstract,
   storyline options, "if the professor asks") and the paste-ready prompts in
   `docs/SANITY_CHECK_2026-07-06.md` that still described the retired
   high-recall-screen / Stage-2-vetoes framing.
5. Brought `docs/ARCHITECTURE.md` up to date with all of the above, including
   a new §5 entry recording the still-open questions (conditional Stage 3
   triggering, matched-split question, what Stage 3 reasons over, per-subgroup
   context) as explicitly deferred, not decided.

## Key decisions made

- The control arm is the single most load-bearing ablation arm and did not
  need a separate N1 runner script — implemented inline in
  `evaluate_pipeline.py` as `_control_arm_report`, reusing
  `select_threshold_for_capacity` and the existing `_pipeline_report` shape.
- `verify_quote` is a plain verbatim substring check, not a semantic
  entailment check. Deliberately cheap: it catches fabricated quotes, which
  is the failure mode that matters for human review, not misleading-but-real
  quotes.
- All 5 of Stage 3's required JSON fields (decision, domain, quote,
  quote-non-empty, planned_return) are enforced at the same level in
  `_parse_response` — a missing quote or missing planned_return fails
  parsing exactly like a missing decision does. No silent partial-success
  state.
- Did **not** implement conditional Stage 3 triggering (auditing only
  discordant cases), even though I had flagged it in the prior session as
  "the next BUILD NOW item" — the user explicitly said they want to evaluate
  that question themselves before deciding. Recorded it as open in
  `docs/ARCHITECTURE.md` §5 instead of building it.
- `docs/THESIS_NARRATIVE.md`'s abstract numbers (AUROC 0.706, recall 0.85,
  "+21% precision") were replaced with an explicit retrain-pending
  placeholder rather than deleted or left as-is — they're stale-model
  artifacts (the old recall-floor Stage 1 and pre-fairness-rebuild Stage 2),
  not current results, and leaving them in would risk them being copy-pasted
  into the actual thesis as if current.

## Files changed

- `src/model/evaluate_pipeline.py` — new `_control_arm_report`; wired into
  `evaluate_pipeline()`; new import
  `from src.model.metrics import select_threshold_for_capacity`; docstring
  updated ("four evaluation layers").
- `src/stage3/explain.py` — new `PLANNED_RETURN_ANSWERS` constant, new
  `verify_quote()`, `_PARSE_FAILURE`/`_parse_response` extended, `_USER_TEMPLATE`
  extended to 5 required fields, `call_llm` signature extended with
  `note_text` to compute `quote_verified`.
- `src/stage3/models.py` — `ExplanationResult` gained `supporting_quote`,
  `quote_verified`, `planned_return`.
- `src/stage3/pipeline.py` — `explain_patient` passes `note_text` to
  `call_llm` and populates the 3 new `ExplanationResult` fields.
- `src/stage3/batch.py` — `_OUTPUT_FIELDS` extended with the 3 new columns.
- `tests/test_evaluate_pipeline.py` — new `subgroups_20` fixture,
  `test_control_arm_*` tests, import updated.
- `tests/test_stage3_explain.py` — extended for quote/planned_return
  validation and `verify_quote`.
- `tests/test_stage3_batch.py` — `_fake_result()` updated with the new
  fields.
- `docs/THESIS_NARRATIVE.md` — §1, §4 (abstract), §2 (storyline options,
  recommended blend), §3 (chapter table, storytelling devices), §5 ("if the
  professor asks"), top banner — rewritten to match the capacity-constrained
  screen / independent second opinion / independent auditor framing.
- `docs/SANITY_CHECK_2026-07-06.md` — the "include in every session" prompt
  and the N5 literature-positioning prompt updated to the current framing
  (the rest of the doc is left as historical record, per its own superseded
  banner).
- `docs/ARCHITECTURE.md` — §2 (Layer 3, RQ2), §4 (N1 bullet), §5 (new open
  questions), §6 (planned_return cross-reference), new §3 session-18 entry,
  top banner date.

## What works now

- All 5 Stage 3 output fields (decision, domain, justification, quote,
  quote_verified, planned_return) round-trip through `explain_patient` →
  `ExplanationResult` → `batch.py`'s CSV output.
- `_control_arm_report` produces a same-shape report to the main pipeline
  report, at the same realized alert rate, so the two are directly
  comparable without any extra normalization step.
- Full test suite: 121 passed, 1 skipped. Pylint: 10.00/10.
- `docs/THESIS_NARRATIVE.md` and `docs/SANITY_CHECK_2026-07-06.md` no longer
  contain "wide net" / "high-recall screen" / "vetoes" / "second reader on a
  high-recall screen" language that contradicts the actual pipeline.

## What's broken / open problems

- All of session 17's known gaps are still open (macOS `stage2.predict`
  standalone-CLI segfault, unwired bootstrap CIs in `evaluate_pipeline.py`,
  frontend `confirmedOnly` default still `true`, etc.) — untouched this
  session.
- Conditional Stage 3 triggering, the Stage 1/2 matched-split question, what
  exactly Stage 3 should reason over, and per-subgroup context for Stage 3 —
  all four raised this round, none decided or implemented. See
  `docs/ARCHITECTURE.md` §5.
- `docs/SANITY_CHECK_2026-07-06.md`'s body (C1-C12/N1-N8 task descriptions,
  outside the two prompts touched today) still has scattered "second
  reader"/"vetoes" language — left as-is deliberately, since the doc's own
  banner already marks it superseded/historical and the user's instruction
  was to build, not to do a full historical-doc rewrite.
- `_control_arm_report` has not been run at scale yet — no GPU retrain has
  happened, so there's no real control-arm number to report, only the
  passing unit tests confirming the function's shape and threshold logic.

## Next steps for whoever continues

- Wait for the user to evaluate the four open architecture questions
  (`docs/ARCHITECTURE.md` §5) before touching conditional triggering, split
  parity, Stage 3's evidence bundle, or per-subgroup context.
- When GPU time is available: retrain Stage 1 and Stage 2, then run
  `predict_stage2_all` → `compare_layers.py` → `batch.py` →
  `evaluate_pipeline.py` end-to-end to get the first real control-arm and
  RQ1/RQ2 numbers, and replace the abstract's retrain-pending placeholder
  with actual figures.
- Once real numbers exist, run `batch.py --sweep` to validate
  `discordance_displacement_pp` empirically instead of leaving it at the
  provisional 20pp.
