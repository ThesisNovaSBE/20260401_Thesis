# Session Log

- **Date:** 2026-06-24
- **Author:** Martin Schmitz (with Claude)
- **Branch:** main

## What I worked on

Initial repository skeleton for the readmission prediction pipeline.

## Key decisions made

- Two-stage pipeline architecture: high-recall classical ML (Stage 1) → Bio_ClinicalBERT false-positive pruning (Stage 2), with optional Ollama explanation (Stage 3).
- Real MIMIC-IV data is NEVER committed. All data paths are configured via `.env` / `config.yaml`, gitignored.
- Dual-mode data: if real MIMIC paths exist locally, use them; otherwise fall back to synthetic data with identical column schemas. This lets anyone develop and test without credentialed access.
- Config precedence: `.env` overrides `config.yaml` values.
- Column schemas defined in `src/schemas.py` as the contract between data stages.

## Files changed

- Created: `.gitignore`, `.env.example`, `config.yaml`, `requirements.txt`, `README.md`, `MODEL_CARD.md`, `PROJECT_TLDR.md`
- Created: `src/config.py`, `src/schemas.py`
- Created: `src/data/synthetic.py` (fully implemented), `src/data/cohort.py`, `src/data/features.py` (placeholders)
- Created: `src/model/train.py`, `src/model/evaluate.py`, `src/model/predict.py` (placeholders)
- Created: `setup_demo.py`, `data/README.md`, `data/.gitkeep`, `models/.gitkeep`, `notebooks/.gitkeep`
- Created: `sessions/` directory with template

## What works now

- `python setup_demo.py` generates synthetic data (patients, admissions, labs, vitals, feature matrix with readmission labels) and writes to `data/synthetic/`.
- `python -m src.data.synthetic` also works standalone with `--n` and `--seed` flags.
- Config loading from `config.yaml` + `.env` with `src.config.load_config()`.
- `.gitignore` blocks all data files, model artifacts, secrets, and MIMIC paths.

## What's broken / open problems

- `src/data/cohort.py` and `src/data/features.py` are stubs — real MIMIC-IV extraction not implemented yet.
- `src/model/train.py`, `evaluate.py`, `predict.py` are stubs.
- No tests written yet.
- `requirements.txt` not yet installed / tested in a fresh venv.

## Next steps for whoever continues

- Install dependencies in a fresh venv and run `python setup_demo.py` to verify.
- Implement Stage 1 training loop in `src/model/train.py` using the synthetic feature matrix.
- Begin real MIMIC-IV cohort extraction in `src/data/cohort.py` (requires credentialed data).
- Add first notebook in `notebooks/` for EDA on the synthetic data.
